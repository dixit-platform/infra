name: Promote

# ─────────────────────────────────────────────────────────────
# TRIGGER 1: Automatic — dev-v* tag created by cd.yml
#            → promotes to stage automatically
#
# TRIGGER 2: Manual — workflow_dispatch
#            → HOW TO USE:
#              1. Click "Run workflow"
#              2. Under "Use workflow from" → select the TAG
#                 e.g. Tag: stage-v1.0.3  or  Tag: v1.0.0
#              3. Select target environment + type CONFIRM
#            No need to type the tag again — github.ref_name
#            is already the tag you selected.
# ─────────────────────────────────────────────────────────────
on:
  push:
    tags:
      - 'dev-v*'             # auto-promote dev release to stage

  workflow_dispatch:
    inputs:
      environment:
        description: 'Target environment'
        required: true
        default: 'prod'
        type: choice
        options:
          - stage
          - prod
      confirm:
        description: 'Type CONFIRM to proceed (required for prod)'
        required: true

permissions:
  contents: write
  id-token: write

jobs:
  # ──────────────────────────────────────────────
  # 1. Guard — validate inputs and resolve context
  # ──────────────────────────────────────────────
  guard:
    name: Validate & Resolve
    runs-on: [self-hosted, Linux, X64, terraform]
    if: github.event.repository.full_name == github.repository
    outputs:
      tag:         ${{ steps.resolve.outputs.tag }}
      environment: ${{ steps.resolve.outputs.environment }}
      tf_dir:      ${{ steps.resolve.outputs.tf_dir }}

    steps:
      - name: Resolve tag and environment
        id: resolve
        run: |
          # ── Tag is always github.ref_name ─────────────────────
          # For push:             the pushed tag   (e.g. dev-v1.0.3)
          # For workflow_dispatch: the tag selected in "Use workflow from"
          #                        (e.g. stage-v1.0.3 or v1.0.0)
          TAG="${{ github.ref_name }}"

          if [[ "${{ github.event_name }}" == "push" ]]; then
            # Automatic path: dev-v* tag pushed by cd.yml → always stage
            ENVIRONMENT="stage"
          else
            # Manual path: user selects environment in dispatch input
            ENVIRONMENT="${{ inputs.environment }}"
          fi

          # ── Validate confirmation for prod ────────────────────
          if [[ "$ENVIRONMENT" == "prod" ]]; then
            if [[ "${{ inputs.confirm }}" != "CONFIRM" ]]; then
              echo " Production deployment requires typing CONFIRM."
              echo "   You typed: '${{ inputs.confirm }}'"
              exit 1
            fi
          fi

          # ── Validate tag matches target environment ───────────
          # dev-v*   → stage only
          # stage-v* → prod only
          # v*       → prod only (main branch release from cd.yml)
          if [[ "$ENVIRONMENT" == "stage" && "$TAG" != dev-v* ]]; then
            echo " Only dev-v* tags can be promoted to stage. Got: $TAG"
            exit 1
          fi

          if [[ "$ENVIRONMENT" == "prod" && "$TAG" != stage-v* && "$TAG" != v[0-9]* ]]; then
            echo " Only stage-v* or v* tags can be promoted to prod. Got: $TAG"
            exit 1
          fi

          # ── Resolve terraform directory ───────────────────────
          if [[ "$ENVIRONMENT" == "prod" ]]; then
            TF_DIR="terraform/environments/prod"
          else
            TF_DIR="terraform/environments/stage"
          fi

          echo "tag=$TAG"                 >> $GITHUB_OUTPUT
          echo "environment=$ENVIRONMENT" >> $GITHUB_OUTPUT
          echo "tf_dir=$TF_DIR"           >> $GITHUB_OUTPUT

          echo "========================================"
          echo "  PROMOTION PLAN"
          echo "  Tag         : $TAG"
          echo "  Environment : $ENVIRONMENT"
          echo "  TF Dir      : $TF_DIR"
          echo "  Triggered by: ${{ github.actor }}"
          echo "========================================"

  # ──────────────────────────────────────────────
  # 2. Deploy — checkout tag, apply, validate
  # ──────────────────────────────────────────────
  deploy:
    name: Deploy ${{ needs.guard.outputs.tag }} → ${{ needs.guard.outputs.environment }}
    needs: guard
    runs-on: [self-hosted, Linux, X64, terraform]
    environment: ${{ needs.guard.outputs.environment }}   # triggers GitHub approval gate
    env:
      TF_DIR: ${{ needs.guard.outputs.tf_dir }}
      AWS_DEFAULT_REGION: ap-south-1

    steps:
      - name: Checkout at tag
        uses: actions/checkout@v4
        with:
          ref: ${{ needs.guard.outputs.tag }}   # deploy exactly this tag, not HEAD
          fetch-depth: 0

      - name: Verify Terraform
        run: terraform version

      - name: Terraform Init
        working-directory: ${{ env.TF_DIR }}
        run: terraform init -input=false

      - name: Terraform Plan
        working-directory: ${{ env.TF_DIR }}
        run: |
          terraform plan -input=false -out=tfplan
          echo "Plan complete"

      - name: Terraform Apply
        working-directory: ${{ env.TF_DIR }}
        run: |
          echo "========================================"
          echo "  DEPLOYING TO ${{ needs.guard.outputs.environment }}"
          echo "========================================"
          terraform apply -input=false -auto-approve tfplan

      - name: Validate Infrastructure
        working-directory: ${{ env.TF_DIR }}
        run: |
          echo "========================================"
          echo "  INFRASTRUCTURE VALIDATION"
          echo "========================================"
          OUTPUT=$(terraform output 2>&1)
          if echo "$OUTPUT" | grep -q "No outputs found"; then
            echo " No outputs defined yet — skipping output check"
          else
            echo "$OUTPUT"
          fi
          echo "Validation complete"

  # ──────────────────────────────────────────────
  # 3. Tag — create promotion tag after successful deploy
  #    dev-v1.0.3   → stage-v1.0.3
  #    stage-v1.0.3 → v1.0.3  (prod)
  #    v1.0.3       → already a prod tag, no new tag needed
  # ──────────────────────────────────────────────
  tag-release:
    name: Tag Release
    needs: [guard, deploy]
    runs-on: [self-hosted, Linux, X64, terraform]
    if: success() && github.event.repository.full_name == github.repository

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Generate promotion tag
        id: version
        run: |
          SOURCE_TAG="${{ needs.guard.outputs.tag }}"
          ENVIRONMENT="${{ needs.guard.outputs.environment }}"

          # Strip prefix to get bare version number
          VERSION="${SOURCE_TAG#dev-v}"
          VERSION="${VERSION#stage-v}"
          VERSION="${VERSION#v}"

          if [[ "$ENVIRONMENT" == "stage" ]]; then
            NEW_TAG="stage-v${VERSION}"
          elif [[ "$SOURCE_TAG" == v* ]]; then
            # Already a prod v* tag from main — no new tag needed
            echo "tag=" >> $GITHUB_OUTPUT
            echo " Source is already a prod tag — no new tag will be created"
            exit 0
          else
            NEW_TAG="v${VERSION}"
          fi

          echo "tag=$NEW_TAG" >> $GITHUB_OUTPUT
          echo "Promotion: $SOURCE_TAG → $NEW_TAG"

      - name: Create and push promotion tag
        if: steps.version.outputs.tag != ''
        run: |
          git config user.name  "${{ github.actor }}"
          git config user.email "${{ github.actor_id }}+${{ github.actor }}@users.noreply.github.com"
          git tag ${{ steps.version.outputs.tag }}
          git push origin ${{ steps.version.outputs.tag }}
          echo "========================================"
          echo "  PROMOTION COMPLETE"
          echo "  Source tag  : ${{ needs.guard.outputs.tag }}"
          echo "  New tag     : ${{ steps.version.outputs.tag }}"
          echo "  Environment : ${{ needs.guard.outputs.environment }}"
          echo "  Deployed by : ${{ github.actor }}"
          echo "========================================"